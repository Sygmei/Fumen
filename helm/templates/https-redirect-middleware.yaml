{{- if and .Values.ingress.enabled .Values.ingress.httpsRedirect.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: https-redirect-mw
spec:
  redirectScheme:
    scheme: https
    permanent: true
{{- end }}
